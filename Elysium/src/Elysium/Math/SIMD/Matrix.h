#pragma once

#include "Common.h"
#include "../Matrix.hpp"

Math::Matrix<float, 4, 4> mat_mul(const Math::Matrix<float, 4, 4>& u, const Math::Matrix<float, 4, 4>& b) {

}

Math::Matrix<double, 4, 4> mat_mul(const Math::Matrix<double, 4, 4>& u, const Math::Matrix<double, 4, 4>& b) {

}